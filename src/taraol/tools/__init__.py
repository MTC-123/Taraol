"""Optional agent tools that adopters can use (or model their own on).

The kit's instrumentation captures any tool's I/O via ``Instrument.tool(...)``; these
are convenience implementations. ``search`` needs the ``[search]`` extra (httpx).
"""
