"""Optional framework/transport integrations for the OTel Agent Kit.

Each sub-package here adapts an external system (an A2A transport, a web
framework, and so on) to the kit's core APIs. Integrations are optional and pull
their own extra dependencies (see the package extras).
"""
